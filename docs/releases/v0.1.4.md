# Night Voyager v0.1.4

> Release type: `local synthetic portfolio release`

## Summary

Night Voyager `v0.1.4` 发布 `v0.1.3` 之后已合并的 DRA governed consumer closure、versioned planning revision authority、双语 planning-revision browser proof，以及 React / Next.js / PostCSS maintenance。当前 development 与 release identity 使用 migration `0013`；本次 release-prep 只收口版本、文档与 contracts。

## Completion

- Night Voyager-owned DRA consumer 已覆盖 governed live consumer、capture、recovery、Stage 2–4、evaluation 与 semantic candidate-freeze evidence。producer pin 固定为 exact commit `01ba21f2996769e68cbc88f4bb0596740df27f6b` 和 profile `generic-strict-citation@1`；这是 Night Voyager consumer contract，not a DRA v0.1.6 release capability。
- versioned planning revision authority 保留 predecessor lineage，生成 successor PlanningRun，提供 deterministic old/new comparison，并要求 fresh advisor reauthorization 与 only-current family decision。`zh-CN` 与 `en` browser journey 已有 provider-free proof。
- React / Next.js / PostCSS maintenance 已合并。Dependabot #8 与 Dependabot #9 状态为 `FIXED`；Dependabot #7 仍为 `OPEN`，对应 optional/transitive `sharp` advisory `GHSA-f88m-g3jw-g9cj`。
- full audit 仍包含 dev-only `brace-expansion` / `minimatch` -> ESLint chain，因此本版本明确 not audit-zero。

## Verification

Release candidate 必须通过 focused release/documentation RED -> GREEN、documentation governance、release surface contracts、`uv lock --check`、Ruff、Pyright、frontend lint/typecheck/Vitest/build、`make check`、独立 `make proof`、单次 task-scoped normal Compose proof 与 teardown、clean-tree release verifier，以及历史 release 文档 immutable hash checks。

Gate C、Gate D 与 Gate E 的可复现步骤定义在 [v0.1.4 verification guide](../how-to/verify-v0.1.4-release.md)。本地 release-prep 不创建 annotated tag、GitHub Release，也不执行正式 GitHub-generated source archive 的 Gate E。

## Scope

本 patch release 只发布已合并能力，并把 package identity、current-release entry points、release verifier 与 documentation contracts 收敛到 `v0.1.4`。发布 artifact 仍是 GitHub-generated source archive。release-prep does not change migration, API, runtime behavior, or dependency tree，也不改变 Docker / Compose 或业务行为。

## Risk / Impact

strict live acceptance 仍为 `INCOMPLETE_PENDING_LIVE_ACCEPTANCE`。两次真实 provider attempt 都产生 zero cited Evidence，并在 before candidate import 阶段 fail closed；no third provider attempt，且禁止把本版本解释为新的 provider attempt 或 live acceptance。

本版本是 local synthetic portfolio release：no production deployment，no source-truth or provider-quality claim，no real student or school coverage，no advisor-team adoption，no admissions outcome，no HA or SLA，no business-benefit claim。它也不代表 production tenancy、真实顾问团队、真实学校数据或录取结果。

## Documentation impact

Bilingual README、documentation index、contributor/security entry、current design/status banners、ADR/spec/plan status、release verifier、architecture/unit contracts、release notes 与 verification guide 现在以 `v0.1.4` 为 current release。`v0.1.0` 至 `v0.1.3` 的 published release notes 与 verification guides 保持 byte-identical immutable history。
