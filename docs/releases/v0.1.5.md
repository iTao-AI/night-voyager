# Night Voyager v0.1.5

> Release type: `local synthetic portfolio release`

## Summary

Night Voyager `v0.1.5` 发布 `v0.1.4` 之后已合并的 governed timeline execution、recovery/reassessment authority、session/task/receipt reconciliation，以及 professional presentation 与 evaluator-first DX。当前 release identity 使用 migration `0015`；本次 release-prep 只收口版本、文档与 contracts。

## Completion

- PR #80 发布 Night Voyager-owned governed timeline execution：`/demo/plan` 将不可变 timeline、PostgreSQL-owned checkpoint/date/risk authority、structured family attestation 与 assigned-advisor verification 串成 provider-free browser-to-database path。
- PR #83 与 PR #84 收口 recovery、reassessment、session、task、receipt reconciliation。reload、stale tab、role/session change、receipt-then-GET 与 reassessment stop 均保持 fail closed，不创建 successor business row，也不把同步 guidance 描述为新 `AgentTask`。
- PR #85 发布 professional presentation 与 evaluator-first DX：双语 action hierarchy、responsive/accessibility closure、semantic browser assertions、review evidence、操作 runbook 和 proof-failure reporting 共同提高本地评估可读性；screenshots 仍只是 review evidence。
- PR #78 是 dependency maintenance。Dependabot 的 PostCSS alerts #8/#9 已修复，但 optional/transitive runtime `sharp` advisory `GHSA-f88m-g3jw-g9cj` 仍保持 open；full development audit 仍包含 dev-only `brace-expansion` / `minimatch` -> ESLint chain，因此本版本明确 not audit-zero。
- PR #81 与 PR #82 仅属于 CI verification maintenance：延长 governed Compose proof budget，并行化独立 proof gates；它们不新增产品能力。

## Verification

Release candidate 必须通过 focused release/documentation RED -> GREEN、documentation governance、release surface contracts、`uv lock --check`、Ruff、Pyright、frontend lint/typecheck/Vitest/build、`make db-check`、authoritative `make check`、独立 `make proof`、单次 task-scoped normal Compose proof 与 teardown、clean-tree release verifier，以及 `v0.1.0` 至 `v0.1.4` 历史 release 文档 immutable hash checks。

Gate C、Gate D 与 Gate E 的可复现步骤定义在 [v0.1.5 verification guide](../how-to/verify-v0.1.5-release.md)。本地 release-prep 不创建 annotated tag、GitHub Release，也不执行正式 GitHub-generated source archive 的 Gate E。

## Scope

本 patch release 只发布已合并能力，并把 Python/Web/package-lock/API runtime identity、current-release entry points、release verifier 与 documentation contracts 收敛到 `v0.1.5`。发布 artifact 仍是 GitHub-generated source archive。except for the declared version identities, release-prep does not change migrations, runtime behavior, backend/domain product logic, BFF/API endpoints or schemas, dependency choices, Dockerfiles, or Compose policy。

## Risk / Impact

strict live acceptance 仍为 `INCOMPLETE_PENDING_LIVE_ACCEPTANCE`。既有 two bounded provider attempts 均产生 zero cited Evidence，并在 candidate import 前 fail closed；no third provider attempt，且禁止把本版本解释为新的 provider attempt、provider quality 或 live acceptance。

本版本是 local synthetic portfolio release：no production deployment，no real users，no real schools or student data，no admissions outcome，no business-benefit claim，no HA or SLA。它也不代表 production tenancy、真实顾问团队采用、source-truth coverage 或服务可用性。

## Documentation impact

Bilingual README、documentation index、contributor/security entry、current design/status banners、ADR/spec/plan status、release verifier、architecture/unit contracts、release notes 与 verification guide 现在以 `v0.1.5` 为 current release。`v0.1.0` 至 `v0.1.4` 的 published release notes 与 verification guides 保持 byte-identical immutable history。
