# Night Voyager Native Showcase Closeout Plan

Status: `COMPLETE`
Base: `main@01de938af2faa06f129be581154cb61f51eed5e4`
Scope: presentation evidence and public README ordering only

## Goal

Replace the first README layer's long proof-capture gallery with four concise,
deterministic showcase frames that make the advisor workspace and its normal and
blocked paths legible at their published reading width.

## Locked boundaries

- Reuse the current real presentation routes and deterministic states.
- Keep `advisor-workspace-overview.png`, `advisor-normal-path.png`, and
  `advisor-blocked-recovery.png` at `1600x1000`; keep
  `advisor-workspace-mobile.png` at `390x844`.
- Preserve the same-Case connected flow and the independently seeded plan-execution
  scenario boundary in copy, capture metadata, and screenshots.
- Keep advisor/client approval explicit; label all evidence synthetic/demo.
- Do not change backend, API, domain, schema, migrations, reducers, dependencies,
  release state, or existing historical proof assets.
- Do not publish, push, create a PR, merge, deploy, or clean unrelated resources.

## Execution

1. Add a bounded presentation-only capture contract and focused assertions for the
   four filenames, exact viewports, state sources, dimensions, and public-neutral
   manifest fields.
2. Capture the frames from current deterministic routes/states with existing
   Playwright tooling; compute SHA-256 values and record the source commit/tree,
   route/state, viewport, locale, and synthetic/demo disclosure.
3. Reorder `README.md` and `README_CN.md` as value -> overview -> user/problem ->
   five-stage workflow -> normal/blocked frames -> three engineering judgments ->
   quickstart/architecture/release/detailed proof. Keep existing long captures linked
   only in the detailed proof layer.
4. Update the documentation index and mark this plan `COMPLETE` only after focused
   presentation tests, frontend checks, proportional repository validators, real
   browser QA, image/hash checks, `git diff --check`, and private-marker scans pass.

## Closeout evidence

The local result must end on a clean `codex/` branch with a semantic atomic commit,
exact `HEAD` and tree recorded, no pull request, and a concise mini-retro covering
what was learned and any remaining review gate.

## Mini-retro seed

- The capture-only browser composition must be isolated from the product layout: the
  core Compose stack can start without the `browser-proof` default command, while the
  capture container runs only the explicit showcase test.
- A fresh deterministic stack is part of screenshot truth. The connected normal frame
  and independently seeded blocked frame must be captured from their declared routes,
  then bound to exact viewport dimensions and SHA-256 values in one manifest.
- Remaining gate: repository authority review and separately authorized remote publication;
  this local execution does not create a PR, push, merge, release, or deploy.
